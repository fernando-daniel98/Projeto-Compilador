/* Teste 11: Função recursiva simples */
int fatorial(int n) {
    if (n <= 1) {
        return 1;
    } else {
        return n * fatorial(n - 1);
    }
}

void main(void){
    int numero;
    int resultado;
    
    numero = 5;
    resultado = fatorial(numero);
    
    output(resultado);
}
