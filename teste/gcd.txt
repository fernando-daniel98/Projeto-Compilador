int gcd (int u, int v)
{       
    if (v == 0)
		return u ;
    else
        return gcd(v, u-u/v*v);
	
    /* Coen */
}

void main(void)
{   int x; in$t y;
    x = input(); y = input();
    output(gcd(x,y));
}
